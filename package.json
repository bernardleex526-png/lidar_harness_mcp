{
  "name": "lidar-harness-mcp",
  "version": "0.2.0",
  "description": "LiDAR Harness MCP Server - Incremental verification engine as pluggable middleware for Claude Code, OpenCode, and any MCP client",
  "type": "module",
  "bin": {
    "lidar-harness": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "prepublishOnly": "npm run build",
    "postinstall": "node -e \"try{require('fs').chmodSync('dist/index.js',0o755)}catch(e){}\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.9"
  }
}
